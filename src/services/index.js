const docx = require("./docx");
const generation = require("./generation");
const armdoc = require("./armdoc");
const cleanup = require("./cleanup");

module.exports = {
  docx,
  generation,
  armdoc,
  cleanup,
};
